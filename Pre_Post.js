// Operator	What happens
// i++	use i, then increment
// ++i	increment, then use i

let i = 1;
console.log(i++) //First use than increment.
console.log(i)

let j= 1
console.log(++j) //FIrst increment and than use.
console.log(j)

//In loop incremneting/decrementing is done after each iteration so that's why no affect is being seen.