// This file was generated by Prisma, and assumes you have installed the following:
// npm install --save-dev prisma dotenv
//This loads environment variables from .env
import "dotenv/config";
//defineConfig() helps Prisma understand your configuration with TypeScript type safety.
import { defineConfig } from "prisma/config";
//much better option
if (!process.env["DATABASE_URL"]) {
    throw new Error("DATABASE_URL is not defined");
}
/*
So conceptually it's:

function defineConfig(config) {
  return config
}
  provides type checking
*/
//"Here is how my database project is configured."
export default defineConfig({
    schema: "prisma/schema.prisma", //This tells Prisma where your database schema file lives.
    migrations: {
        path: "prisma/migrations", //This tells Prisma where to store database migration files.
    },
    datasource: {
        url: process.env["DATABASE_URL"], //"Connect to the database using this URL."
    },
});
//! I guarantee this value is not undefined"
/*
What this file actually does

This file tells Prisma:

Where schema is
Where migrations go
How to connect to the database
Load environment variables

So when you run:

npx prisma migrate dev

Prisma reads this file and understands your setup.
*/
//# sourceMappingURL=prisma.config.js.map